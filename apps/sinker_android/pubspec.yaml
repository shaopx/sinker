name: sinker_android
description: Sinker Android receiver - receive files via ADB TCP tunnel.
publish_to: none
version: 1.0.0+1

resolution: workspace

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sinker_core:
    path: ../../packages/sinker_core
  path_provider: ^2.1.5
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
