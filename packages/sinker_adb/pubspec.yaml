name: sinker_adb
description: ADB command wrapper for Sinker - device discovery and port forwarding.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  process_run: ^1.2.1

dev_dependencies:
  test: ^1.25.0
